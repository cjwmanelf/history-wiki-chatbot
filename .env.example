# 이 파일을 복사해 `.env` 로 저장한 뒤 실제 키를 넣으세요.
#   copy .env.example .env      (Windows)
#
# `.env` 는 .gitignore 로 차단되어 있어 저장소에 올라가지 않습니다.
# 키를 파일로 남기고 싶지 않다면 Streamlit 데모의 "⚙️ 설정" 탭에서 직접 입력하세요.
# 그 경우 키는 실행 중인 프로세스 메모리에만 존재하고 디스크에 기록되지 않습니다.

OPENAI_API_KEY=""
